# Maze configuration file
# Lines starting with '#' are comments

width=23   # Maze width (number of cells)
HEIGHT=23  # Maze height (number of cells)

ENTRY=0,9  # Coordinates of maze entrance (x,y)
exit=15,19   # Coordinates of maze exit (x,y)

OUTPUT_file=maze.txt   # Output file name

PErFeCT=True       # Whether the maze is perfect (only one path from ENTRY to EXIT)

# Optional keys
SEED=42    # Random seed for reproducibility
ALGORITHM=recursive_backtracker      # Maze generation algorithm: recursive_backtracker, bfs
DISPLAY_MODE=Terminal