# Maze configuration file
# Lines starting with '#' are comments

WIDTH=20  # Maze width (number of cells)
HEIGHT=20  # Maze height (number of cells)

ENTRY=1,2 # Coordinates of maze entrance (x,y)
EXIT=18,19   # Coordinates of maze exit (x,y)

OUTPUT_file=maze.txt   # Output file name

PERFECT=False     # Whether the maze is perfect (only one path from ENTRY to EXIT)

# Optional keys
SEED=42      # Random seed for reproducibility
ALGORITHM=recursive_backtracker      # Maze generation algorithm: Bfs, recursive_backtracker
DISPLAY_MODE=TERMINAL (ASCII)
